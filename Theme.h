#ifndef THEME_H
#define THEME_H

#include <SFML/Graphics.hpp>

namespace Theme {
    static sf::Color BG      = sf::Color(236,240,241);
    static sf::Color Text    = sf::Color(44,62,80);
    static sf::Color Hint    = sf::Color(149,165,166);
    static sf::Color Button  = sf::Color(52,152,219);
    static sf::Color Hover   = sf::Color(41,128,185);
    static sf::Color Danger  = sf::Color(231,76,60);
    static sf::Color OK      = sf::Color(46,204,113);
    static sf::Color Border      = sf::Color(127,140,141);
    static sf::Color BorderFocus = sf::Color(52,152,219);
}

#endif
