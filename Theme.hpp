#ifndef THEME_HPP
#define THEME_HPP
#include <SFML/Graphics.hpp>

namespace Theme {
   const sf::Color Background(242, 195, 185);

    const sf::Color Surface(229, 171, 160);
    const sf::Color Primary(219, 112, 147); // Hồng đậm
    const sf::Color Secondary(186, 85, 211); // Tím nhạt
    const sf::Color TextDark(75, 0, 130);
    const sf::Color TextLight(100, 80, 100);
    const sf::Color Border(230, 220, 230);
    const sf::Color Error(220, 80, 80);
    const sf::Color Success(60, 180, 100);
}
#endif